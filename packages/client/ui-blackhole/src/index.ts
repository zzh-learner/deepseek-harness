/**
 * Node-half stub for the GARGANTUA black-hole wallpaper: the whole feature
 * is the browser half — a WebGL2 ray-traced background layer registered into
 * `shell.overlay` plus the wallpaper registry entry. Nothing to mount on the
 * host side.
 *
 * @module @deepseek-ai/dsh-client-ui-blackhole
 */

/** Cordis plugin name. */
export const name = 'client-ui-blackhole'

/** No host-side services or effects. */
export function apply(): void {}

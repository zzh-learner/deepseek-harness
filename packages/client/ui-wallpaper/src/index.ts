/**
 * Node-half stub for the wallpaper registry: the whole feature is the browser
 * half — the `wallpaper.registry` service plus the Settings section that
 * reads it. Nothing to mount on the host side.
 *
 * @module @deepseek-ai/dsh-client-ui-wallpaper
 */

/** Cordis plugin name. */
export const name = 'client-ui-wallpaper'

/** No host-side services or effects. */
export function apply(): void {}
